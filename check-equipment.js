const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const equipment = await prisma.equipment.findMany(); console.log('Equipment count:', equipment.length); console.log('First equipment item:', equipment[0]); } catch (error) { console.error('Error:', error); } } main().then(() => prisma.$disconnect()).catch(e => { console.error(e); process.exit(1); });
